spring:
  data:
    mongodb:
      uri: ${MONGODB_URI:mongodb://localhost:27017/shelflife}

google:
  books:
    api:
      key: ${GOOGLE_BOOKS_API_KEY:}

firebase:
  auth:
    enabled: true
    project-id: shelflife-968cf
    service-account-path: classpath:firebase-service-account.json

app:
  cors:
    allowed-origins:
      - http://localhost:3000
